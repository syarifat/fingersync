# ==============================================================================
# Nixpacks Configuration for Coolify Deployment
# Laravel 12 + Vite + Tailwind CSS + MySQL
# ==============================================================================

# Inherit auto-detected packages + add MySQL & GD extensions
[phases.setup]
nixPkgs = ["...", "php82Extensions.pdo_mysql", "php82Extensions.gd"]

# --- Install Dependencies ---
[phases.install]
cmds = [
    "mkdir -p /app/storage/framework/{sessions,views,cache}",
    "mkdir -p /app/storage/logs",
    "mkdir -p /app/bootstrap/cache",
    "composer install --no-dev --optimize-autoloader --no-interaction --prefer-dist",
    "npm ci"
]

# --- Build Assets & Optimize ---
[phases.build]
cmds = [
    "npm run build",
    "php artisan storage:link || true",
    "php artisan optimize || true"
]
dependsOn = ["install"]

# --- Start Command ---
[start]
cmd = "php artisan migrate --force && php artisan storage:link && php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan serve --host=0.0.0.0 --port=80"
